// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain;

import com.app2.app2t.domain.AppParameter;
import com.app2.app2t.domain.AppParameterDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect AppParameterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: AppParameterDataOnDemand: @Component;
    
    private Random AppParameterDataOnDemand.rnd = new SecureRandom();
    
    private List<AppParameter> AppParameterDataOnDemand.data;
    
    public AppParameter AppParameterDataOnDemand.getNewTransientAppParameter(int index) {
        AppParameter obj = new AppParameter();
        setCode(obj, index);
        setCreatedBy(obj, index);
        setCreatedDate(obj, index);
        setParameterDescription(obj, index);
        setStatus(obj, index);
        setUpdatedBy(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void AppParameterDataOnDemand.setCode(AppParameter obj, int index) {
        String code = "code_" + index;
        obj.setCode(code);
    }
    
    public void AppParameterDataOnDemand.setCreatedBy(AppParameter obj, int index) {
        String createdBy = "createdBy_" + index;
        obj.setCreatedBy(createdBy);
    }
    
    public void AppParameterDataOnDemand.setCreatedDate(AppParameter obj, int index) {
        Date createdDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreatedDate(createdDate);
    }
    
    public void AppParameterDataOnDemand.setParameterDescription(AppParameter obj, int index) {
        String parameterDescription = "parameterDescription_" + index;
        obj.setParameterDescription(parameterDescription);
    }
    
    public void AppParameterDataOnDemand.setStatus(AppParameter obj, int index) {
        String status = "status_" + index;
        obj.setStatus(status);
    }
    
    public void AppParameterDataOnDemand.setUpdatedBy(AppParameter obj, int index) {
        String updatedBy = "updatedBy_" + index;
        obj.setUpdatedBy(updatedBy);
    }
    
    public void AppParameterDataOnDemand.setUpdatedDate(AppParameter obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public AppParameter AppParameterDataOnDemand.getSpecificAppParameter(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        AppParameter obj = data.get(index);
        Long id = obj.getId();
        return AppParameter.findAppParameter(id);
    }
    
    public AppParameter AppParameterDataOnDemand.getRandomAppParameter() {
        init();
        AppParameter obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return AppParameter.findAppParameter(id);
    }
    
    public boolean AppParameterDataOnDemand.modifyAppParameter(AppParameter obj) {
        return false;
    }
    
    public void AppParameterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = AppParameter.findAppParameterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'AppParameter' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<AppParameter>();
        for (int i = 0; i < 10; i++) {
            AppParameter obj = getNewTransientAppParameter(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
