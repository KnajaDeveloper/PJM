// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.TypeTask;
import com.app2.app2t.domain.pjm.TypeTaskDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect TypeTaskDataOnDemand_Roo_DataOnDemand {
    
    declare @type: TypeTaskDataOnDemand: @Component;
    
    private Random TypeTaskDataOnDemand.rnd = new SecureRandom();
    
    private List<TypeTask> TypeTaskDataOnDemand.data;
    
    public TypeTask TypeTaskDataOnDemand.getNewTransientTypeTask(int index) {
        TypeTask obj = new TypeTask();
        setCreatedBy(obj, index);
        setCreatedDate(obj, index);
        setStatus(obj, index);
        setTypeTaskCode(obj, index);
        setTypeTaskName(obj, index);
        setUpdatedBy(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void TypeTaskDataOnDemand.setCreatedBy(TypeTask obj, int index) {
        String createdBy = "createdBy_" + index;
        obj.setCreatedBy(createdBy);
    }
    
    public void TypeTaskDataOnDemand.setCreatedDate(TypeTask obj, int index) {
        Date createdDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreatedDate(createdDate);
    }
    
    public void TypeTaskDataOnDemand.setStatus(TypeTask obj, int index) {
        String status = "status_" + index;
        obj.setStatus(status);
    }
    
    public void TypeTaskDataOnDemand.setTypeTaskCode(TypeTask obj, int index) {
        String typeTaskCode = "typeTaskCode_" + index;
        if (typeTaskCode.length() > 15) {
            typeTaskCode = typeTaskCode.substring(0, 15);
        }
        obj.setTypeTaskCode(typeTaskCode);
    }
    
    public void TypeTaskDataOnDemand.setTypeTaskName(TypeTask obj, int index) {
        String typeTaskName = "typeTaskName_" + index;
        if (typeTaskName.length() > 40) {
            typeTaskName = typeTaskName.substring(0, 40);
        }
        obj.setTypeTaskName(typeTaskName);
    }
    
    public void TypeTaskDataOnDemand.setUpdatedBy(TypeTask obj, int index) {
        String updatedBy = "updatedBy_" + index;
        obj.setUpdatedBy(updatedBy);
    }
    
    public void TypeTaskDataOnDemand.setUpdatedDate(TypeTask obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public TypeTask TypeTaskDataOnDemand.getSpecificTypeTask(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        TypeTask obj = data.get(index);
        Long id = obj.getId();
        return TypeTask.findTypeTask(id);
    }
    
    public TypeTask TypeTaskDataOnDemand.getRandomTypeTask() {
        init();
        TypeTask obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return TypeTask.findTypeTask(id);
    }
    
    public boolean TypeTaskDataOnDemand.modifyTypeTask(TypeTask obj) {
        return false;
    }
    
    public void TypeTaskDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = TypeTask.findTypeTaskEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'TypeTask' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<TypeTask>();
        for (int i = 0; i < 10; i++) {
            TypeTask obj = getNewTransientTypeTask(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
