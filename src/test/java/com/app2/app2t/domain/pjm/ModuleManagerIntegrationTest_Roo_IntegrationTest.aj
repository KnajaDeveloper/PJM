// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.ModuleManager;
import com.app2.app2t.domain.pjm.ModuleManagerDataOnDemand;
import com.app2.app2t.domain.pjm.ModuleManagerIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ModuleManagerIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ModuleManagerIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ModuleManagerIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ModuleManagerIntegrationTest: @Transactional;
    
    @Autowired
    ModuleManagerDataOnDemand ModuleManagerIntegrationTest.dod;
    
    @Test
    public void ModuleManagerIntegrationTest.testCountModuleManagers() {
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", dod.getRandomModuleManager());
        long count = ModuleManager.countModuleManagers();
        Assert.assertTrue("Counter for 'ModuleManager' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testFindModuleManager() {
        ModuleManager obj = dod.getRandomModuleManager();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to provide an identifier", id);
        obj = ModuleManager.findModuleManager(id);
        Assert.assertNotNull("Find method for 'ModuleManager' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ModuleManager' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testFindAllModuleManagers() {
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", dod.getRandomModuleManager());
        long count = ModuleManager.countModuleManagers();
        Assert.assertTrue("Too expensive to perform a find all test for 'ModuleManager', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ModuleManager> result = ModuleManager.findAllModuleManagers();
        Assert.assertNotNull("Find all method for 'ModuleManager' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ModuleManager' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testFindModuleManagerEntries() {
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", dod.getRandomModuleManager());
        long count = ModuleManager.countModuleManagers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ModuleManager> result = ModuleManager.findModuleManagerEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ModuleManager' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ModuleManager' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testFlush() {
        ModuleManager obj = dod.getRandomModuleManager();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to provide an identifier", id);
        obj = ModuleManager.findModuleManager(id);
        Assert.assertNotNull("Find method for 'ModuleManager' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyModuleManager(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ModuleManager' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testMergeUpdate() {
        ModuleManager obj = dod.getRandomModuleManager();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to provide an identifier", id);
        obj = ModuleManager.findModuleManager(id);
        boolean modified =  dod.modifyModuleManager(obj);
        Integer currentVersion = obj.getVersion();
        ModuleManager merged = (ModuleManager)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ModuleManager' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", dod.getRandomModuleManager());
        ModuleManager obj = dod.getNewTransientModuleManager(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ModuleManager' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ModuleManager' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ModuleManagerIntegrationTest.testRemove() {
        ModuleManager obj = dod.getRandomModuleManager();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleManager' failed to provide an identifier", id);
        obj = ModuleManager.findModuleManager(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ModuleManager' with identifier '" + id + "'", ModuleManager.findModuleManager(id));
    }
    
}
