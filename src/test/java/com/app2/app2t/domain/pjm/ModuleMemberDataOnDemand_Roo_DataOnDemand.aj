// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.ModuleMember;
import com.app2.app2t.domain.pjm.ModuleMemberDataOnDemand;
import com.app2.app2t.domain.pjm.ModuleProjectDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ModuleMemberDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ModuleMemberDataOnDemand: @Component;
    
    private Random ModuleMemberDataOnDemand.rnd = new SecureRandom();
    
    private List<ModuleMember> ModuleMemberDataOnDemand.data;
    
    @Autowired
    ModuleProjectDataOnDemand ModuleMemberDataOnDemand.moduleProjectDataOnDemand;
    
    public ModuleMember ModuleMemberDataOnDemand.getNewTransientModuleMember(int index) {
        ModuleMember obj = new ModuleMember();
        setCreatedBy(obj, index);
        setCreatedDate(obj, index);
        setEmpCode(obj, index);
        setStatus(obj, index);
        setUpdatedBy(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void ModuleMemberDataOnDemand.setCreatedBy(ModuleMember obj, int index) {
        String createdBy = "createdBy_" + index;
        obj.setCreatedBy(createdBy);
    }
    
    public void ModuleMemberDataOnDemand.setCreatedDate(ModuleMember obj, int index) {
        Date createdDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreatedDate(createdDate);
    }
    
    public void ModuleMemberDataOnDemand.setEmpCode(ModuleMember obj, int index) {
        String empCode = "empCode_" + index;
        if (empCode.length() > 15) {
            empCode = empCode.substring(0, 15);
        }
        obj.setEmpCode(empCode);
    }
    
    public void ModuleMemberDataOnDemand.setStatus(ModuleMember obj, int index) {
        String status = "status_" + index;
        obj.setStatus(status);
    }
    
    public void ModuleMemberDataOnDemand.setUpdatedBy(ModuleMember obj, int index) {
        String updatedBy = "updatedBy_" + index;
        obj.setUpdatedBy(updatedBy);
    }
    
    public void ModuleMemberDataOnDemand.setUpdatedDate(ModuleMember obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public ModuleMember ModuleMemberDataOnDemand.getSpecificModuleMember(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ModuleMember obj = data.get(index);
        Long id = obj.getId();
        return ModuleMember.findModuleMember(id);
    }
    
    public ModuleMember ModuleMemberDataOnDemand.getRandomModuleMember() {
        init();
        ModuleMember obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ModuleMember.findModuleMember(id);
    }
    
    public boolean ModuleMemberDataOnDemand.modifyModuleMember(ModuleMember obj) {
        return false;
    }
    
    public void ModuleMemberDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ModuleMember.findModuleMemberEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ModuleMember' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ModuleMember>();
        for (int i = 0; i < 10; i++) {
            ModuleMember obj = getNewTransientModuleMember(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
