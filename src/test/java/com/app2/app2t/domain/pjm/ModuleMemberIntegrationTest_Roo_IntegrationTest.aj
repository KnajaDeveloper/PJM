// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.ModuleMember;
import com.app2.app2t.domain.pjm.ModuleMemberDataOnDemand;
import com.app2.app2t.domain.pjm.ModuleMemberIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ModuleMemberIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ModuleMemberIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ModuleMemberIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ModuleMemberIntegrationTest: @Transactional;
    
    @Autowired
    ModuleMemberDataOnDemand ModuleMemberIntegrationTest.dod;
    
    @Test
    public void ModuleMemberIntegrationTest.testCountModuleMembers() {
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", dod.getRandomModuleMember());
        long count = ModuleMember.countModuleMembers();
        Assert.assertTrue("Counter for 'ModuleMember' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testFindModuleMember() {
        ModuleMember obj = dod.getRandomModuleMember();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to provide an identifier", id);
        obj = ModuleMember.findModuleMember(id);
        Assert.assertNotNull("Find method for 'ModuleMember' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ModuleMember' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testFindAllModuleMembers() {
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", dod.getRandomModuleMember());
        long count = ModuleMember.countModuleMembers();
        Assert.assertTrue("Too expensive to perform a find all test for 'ModuleMember', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ModuleMember> result = ModuleMember.findAllModuleMembers();
        Assert.assertNotNull("Find all method for 'ModuleMember' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ModuleMember' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testFindModuleMemberEntries() {
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", dod.getRandomModuleMember());
        long count = ModuleMember.countModuleMembers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ModuleMember> result = ModuleMember.findModuleMemberEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ModuleMember' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ModuleMember' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testFlush() {
        ModuleMember obj = dod.getRandomModuleMember();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to provide an identifier", id);
        obj = ModuleMember.findModuleMember(id);
        Assert.assertNotNull("Find method for 'ModuleMember' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyModuleMember(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ModuleMember' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testMergeUpdate() {
        ModuleMember obj = dod.getRandomModuleMember();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to provide an identifier", id);
        obj = ModuleMember.findModuleMember(id);
        boolean modified =  dod.modifyModuleMember(obj);
        Integer currentVersion = obj.getVersion();
        ModuleMember merged = (ModuleMember)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ModuleMember' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", dod.getRandomModuleMember());
        ModuleMember obj = dod.getNewTransientModuleMember(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ModuleMember' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ModuleMember' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ModuleMemberIntegrationTest.testRemove() {
        ModuleMember obj = dod.getRandomModuleMember();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ModuleMember' failed to provide an identifier", id);
        obj = ModuleMember.findModuleMember(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ModuleMember' with identifier '" + id + "'", ModuleMember.findModuleMember(id));
    }
    
}
