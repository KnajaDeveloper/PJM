// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.ImportanceTask;
import com.app2.app2t.domain.pjm.ImportanceTaskDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.stereotype.Component;

privileged aspect ImportanceTaskDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ImportanceTaskDataOnDemand: @Component;
    
    private Random ImportanceTaskDataOnDemand.rnd = new SecureRandom();
    
    private List<ImportanceTask> ImportanceTaskDataOnDemand.data;
    
    public ImportanceTask ImportanceTaskDataOnDemand.getNewTransientImportanceTask(int index) {
        ImportanceTask obj = new ImportanceTask();
        setCreatedBy(obj, index);
        setCreatedDate(obj, index);
        setImportanceTaskCode(obj, index);
        setImportanceTaskName(obj, index);
        setStatus(obj, index);
        setUpdatedBy(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void ImportanceTaskDataOnDemand.setCreatedBy(ImportanceTask obj, int index) {
        String createdBy = "createdBy_" + index;
        obj.setCreatedBy(createdBy);
    }
    
    public void ImportanceTaskDataOnDemand.setCreatedDate(ImportanceTask obj, int index) {
        Date createdDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreatedDate(createdDate);
    }
    
    public void ImportanceTaskDataOnDemand.setImportanceTaskCode(ImportanceTask obj, int index) {
        String importanceTaskCode = "importanceTaskCode_" + index;
        obj.setImportanceTaskCode(importanceTaskCode);
    }
    
    public void ImportanceTaskDataOnDemand.setImportanceTaskName(ImportanceTask obj, int index) {
        String importanceTaskName = "importanceTaskName_" + index;
        obj.setImportanceTaskName(importanceTaskName);
    }
    
    public void ImportanceTaskDataOnDemand.setStatus(ImportanceTask obj, int index) {
        String status = "status_" + index;
        obj.setStatus(status);
    }
    
    public void ImportanceTaskDataOnDemand.setUpdatedBy(ImportanceTask obj, int index) {
        String updatedBy = "updatedBy_" + index;
        obj.setUpdatedBy(updatedBy);
    }
    
    public void ImportanceTaskDataOnDemand.setUpdatedDate(ImportanceTask obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public ImportanceTask ImportanceTaskDataOnDemand.getSpecificImportanceTask(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ImportanceTask obj = data.get(index);
        Long id = obj.getId();
        return ImportanceTask.findImportanceTask(id);
    }
    
    public ImportanceTask ImportanceTaskDataOnDemand.getRandomImportanceTask() {
        init();
        ImportanceTask obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return ImportanceTask.findImportanceTask(id);
    }
    
    public boolean ImportanceTaskDataOnDemand.modifyImportanceTask(ImportanceTask obj) {
        return false;
    }
    
    public void ImportanceTaskDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = ImportanceTask.findImportanceTaskEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ImportanceTask' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ImportanceTask>();
        for (int i = 0; i < 10; i++) {
            ImportanceTask obj = getNewTransientImportanceTask(i);
            try {
                obj.persist();
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            obj.flush();
            data.add(obj);
        }
    }
    
}
