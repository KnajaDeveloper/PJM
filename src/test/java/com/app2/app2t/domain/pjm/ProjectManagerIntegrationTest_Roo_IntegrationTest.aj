// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain.pjm;

import com.app2.app2t.domain.pjm.ProjectManager;
import com.app2.app2t.domain.pjm.ProjectManagerDataOnDemand;
import com.app2.app2t.domain.pjm.ProjectManagerIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ProjectManagerIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ProjectManagerIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ProjectManagerIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ProjectManagerIntegrationTest: @Transactional;
    
    @Autowired
    ProjectManagerDataOnDemand ProjectManagerIntegrationTest.dod;
    
    @Test
    public void ProjectManagerIntegrationTest.testCountProjectManagers() {
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", dod.getRandomProjectManager());
        long count = ProjectManager.countProjectManagers();
        Assert.assertTrue("Counter for 'ProjectManager' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testFindProjectManager() {
        ProjectManager obj = dod.getRandomProjectManager();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to provide an identifier", id);
        obj = ProjectManager.findProjectManager(id);
        Assert.assertNotNull("Find method for 'ProjectManager' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ProjectManager' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testFindAllProjectManagers() {
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", dod.getRandomProjectManager());
        long count = ProjectManager.countProjectManagers();
        Assert.assertTrue("Too expensive to perform a find all test for 'ProjectManager', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ProjectManager> result = ProjectManager.findAllProjectManagers();
        Assert.assertNotNull("Find all method for 'ProjectManager' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ProjectManager' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testFindProjectManagerEntries() {
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", dod.getRandomProjectManager());
        long count = ProjectManager.countProjectManagers();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ProjectManager> result = ProjectManager.findProjectManagerEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ProjectManager' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ProjectManager' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testFlush() {
        ProjectManager obj = dod.getRandomProjectManager();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to provide an identifier", id);
        obj = ProjectManager.findProjectManager(id);
        Assert.assertNotNull("Find method for 'ProjectManager' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyProjectManager(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ProjectManager' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testMergeUpdate() {
        ProjectManager obj = dod.getRandomProjectManager();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to provide an identifier", id);
        obj = ProjectManager.findProjectManager(id);
        boolean modified =  dod.modifyProjectManager(obj);
        Integer currentVersion = obj.getVersion();
        ProjectManager merged = (ProjectManager)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ProjectManager' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", dod.getRandomProjectManager());
        ProjectManager obj = dod.getNewTransientProjectManager(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ProjectManager' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ProjectManager' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ProjectManagerIntegrationTest.testRemove() {
        ProjectManager obj = dod.getRandomProjectManager();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ProjectManager' failed to provide an identifier", id);
        obj = ProjectManager.findProjectManager(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ProjectManager' with identifier '" + id + "'", ProjectManager.findProjectManager(id));
    }
    
}
