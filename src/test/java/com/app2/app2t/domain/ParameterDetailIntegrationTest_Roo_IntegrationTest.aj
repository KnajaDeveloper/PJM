// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.app2.app2t.domain;

import com.app2.app2t.domain.ParameterDetail;
import com.app2.app2t.domain.ParameterDetailDataOnDemand;
import com.app2.app2t.domain.ParameterDetailIntegrationTest;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ParameterDetailIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ParameterDetailIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ParameterDetailIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ParameterDetailIntegrationTest: @Transactional;
    
    @Autowired
    ParameterDetailDataOnDemand ParameterDetailIntegrationTest.dod;
    
    @Test
    public void ParameterDetailIntegrationTest.testCountParameterDetails() {
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", dod.getRandomParameterDetail());
        long count = ParameterDetail.countParameterDetails();
        Assert.assertTrue("Counter for 'ParameterDetail' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testFindParameterDetail() {
        ParameterDetail obj = dod.getRandomParameterDetail();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to provide an identifier", id);
        obj = ParameterDetail.findParameterDetail(id);
        Assert.assertNotNull("Find method for 'ParameterDetail' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ParameterDetail' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testFindAllParameterDetails() {
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", dod.getRandomParameterDetail());
        long count = ParameterDetail.countParameterDetails();
        Assert.assertTrue("Too expensive to perform a find all test for 'ParameterDetail', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ParameterDetail> result = ParameterDetail.findAllParameterDetails();
        Assert.assertNotNull("Find all method for 'ParameterDetail' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ParameterDetail' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testFindParameterDetailEntries() {
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", dod.getRandomParameterDetail());
        long count = ParameterDetail.countParameterDetails();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ParameterDetail> result = ParameterDetail.findParameterDetailEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ParameterDetail' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ParameterDetail' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testFlush() {
        ParameterDetail obj = dod.getRandomParameterDetail();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to provide an identifier", id);
        obj = ParameterDetail.findParameterDetail(id);
        Assert.assertNotNull("Find method for 'ParameterDetail' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyParameterDetail(obj);
        Integer currentVersion = obj.getVersion();
        obj.flush();
        Assert.assertTrue("Version for 'ParameterDetail' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testMergeUpdate() {
        ParameterDetail obj = dod.getRandomParameterDetail();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to provide an identifier", id);
        obj = ParameterDetail.findParameterDetail(id);
        boolean modified =  dod.modifyParameterDetail(obj);
        Integer currentVersion = obj.getVersion();
        ParameterDetail merged = (ParameterDetail)obj.merge();
        obj.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ParameterDetail' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testPersist() {
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", dod.getRandomParameterDetail());
        ParameterDetail obj = dod.getNewTransientParameterDetail(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ParameterDetail' identifier to be null", obj.getId());
        try {
            obj.persist();
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        obj.flush();
        Assert.assertNotNull("Expected 'ParameterDetail' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ParameterDetailIntegrationTest.testRemove() {
        ParameterDetail obj = dod.getRandomParameterDetail();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ParameterDetail' failed to provide an identifier", id);
        obj = ParameterDetail.findParameterDetail(id);
        obj.remove();
        obj.flush();
        Assert.assertNull("Failed to remove 'ParameterDetail' with identifier '" + id + "'", ParameterDetail.findParameterDetail(id));
    }
    
}
